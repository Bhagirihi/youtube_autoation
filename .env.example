# Gemini — story and TTS can use different keys; multiple TTS keys rotate per paragraph
GEMINI_MASTER_API_KEY=your_gemini_api_key
# Optional: dedicated key for story (else first working key above is used)
# GEMINI_STORY_API_KEY=your_story_key
# Story: if set (e.g. USE_TWO_STEP_STORY=1) → single prompt (story.prompt.txt). If not set → two-step (long story ~5000 chars + metadata).
# USE_TWO_STEP_STORY=1
# Optional: single key for all TTS (else all GEMINI_* keys rotate per paragraph)
# GEMINI_TTS_API_KEY=your_tts_key
# Extra keys for TTS rotation (paragraph 0 → key 1, paragraph 1 → key 2, etc.)
# GEMINI_MASTER_API_KEY_1=...
# GEMINI_MASTER_API_KEY_2=...

# Force a specific TTS provider (optional)
# USE_GEMINI_TTS=1   → use Gemini TTS only
# USE_SARVAM_TTS=1   → use Sarvam AI TTS only (Hindi/Indian languages)
# USE_SARVAM_TTS=1 + SARVAM_API_KEY=... required for Sarvam
# SARVAM_API_KEY=your_sarvam_api_key
# Optional: SARVAM_SPEAKER=shubh SARVAM_PACE=0.98 SARVAM_LANGUAGE=hi-IN SARVAM_MODEL=bulbul:v3

# Inworld AI TTS (USE_INWORLD_TTS=1)
# Option A: pre-encoded Basic auth string
# INWORLD_BASIC_AUTH=your_base64_string
# INWORLD_BASIC_AUTH_2=... (second key; use with INWORLD_VOICE_ID_2)
# Option B: API key. If Inworld uses key:secret, also set INWORLD_API_SECRET.
# INWORLD_API_KEY=your_api_key
# INWORLD_API_SECRET=your_secret
# Voice per key: INWORLD_VOICE_ID (key 1), INWORLD_VOICE_ID_2 (key 2), ...
# Optional: INWORLD_MODEL_ID=inworld-tts-1.5-max INWORLD_SPEAKING_RATE=0.97 INWORLD_TEMPERATURE=0.87

# ElevenLabs TTS (default when no USE_*_TTS and key is valid)
ELEVENLABS_API_KEY=your_elevenlabs_api_key
# Optional: voice ID (default: JBFqnCBsd6RMkjVDRZzb)
# ELEVENLABS_VOICE_ID=your_voice_id

# Optional: background music (merged with TTS at low volume). Path to .mp3, .mov, etc.
# BGM_PATH=/Users/dhruvdave/Desktop/musicGPT.mov
# BGM volume 0–1 (default 0.04 = 4%)
# BGM_VOLUME=0.04

# Optional: slower narration for horror (0.5–1.5; e.g. 0.96 = 4% slower). Applied after TTS.
# TTS_PACE=0.96
# Optional: darker color curve on final video (cold horror look).
# HORROR_COLOR=1

# YouTube: direct upload (use "Connect YouTube" in app first)
# Option A — JSON in env (good for CI; no auth/ files needed):
# YT_CLIENT_SECRET_JSON={"installed":{"client_id":"...","client_secret":"...","redirect_uris":["http://localhost:3000/api/youtube/callback"]}}
# YT_TOKEN_JSON={"refresh_token":"1//0g...","access_token":"...",...}
# YT_REDIRECT_URI=http://localhost:3000/api/youtube/callback
# Option B — Use client_secret JSON file in auth/ (set to your actual filename):
# YT_CLIENT_SECRET_PATH=auth/client_secret_xxxxx.apps.googleusercontent.com.json
# Redirect URI must be added in Google Cloud Console and match exactly:
YT_REDIRECT_URI=http://localhost:3000/api/youtube/callback
# After "Connect YouTube", tokens are saved to auth/HorrorPodcastAdda.token.json (no .env needed).
# Option C — Or set credentials in .env:
# YT_CLIENT_ID=944372979454-....apps.googleusercontent.com
# YT_CLIENT_SECRET=GOCSPX-...
# YT_REDIRECT_URI=http://localhost:3000/api/youtube/callback
# YT_REFRESH_TOKEN=... (or use token file in auth/)
# Optional: YT_PRIVACY_STATUS=private | unlisted | public (default: public)
# Schedule next video: last published + 2 days at 9 PM (e.g. last 13 Feb → next 15 Feb 9 PM)
# YT_SCHEDULE_NEXT=1
# YT_SCHEDULE_TIMEZONE=Asia/Kolkata
# YT_SCHEDULE_HOUR=21
# YT_SCHEDULE_MINUTE=0
# YT_SCHEDULE_DAYS_AFTER=2
